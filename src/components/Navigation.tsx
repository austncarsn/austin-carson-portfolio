import BarcodeNav from './BarcodeNav';

/**
 * Site Navigation — Barcode navigation system
 * Consolidated into BarcodeNav component with raised capsule buttons
 */
export default function Navigation() {
  return <BarcodeNav />;
}
